# Compose file for MicroXRCEDSS

name: qadt-deployment-xrce

services:
  uxrce:
    extends:
      file: compose.deployment.yml
      service: base

    container_name: qadt-uxrce-agent
    
    command: >
      MicroXRCEAgent udp4 -p 8888