services:
  dev:
    environment:
      DISPLAY: "${DISPLAY}"
      WAYLAND_DISPLAY: "${WAYLAND_DISPLAY}"
      XDG_RUNTIME_DIR: "${XDG_RUNTIME_DIR}"
      PULSE_SERVER: "unix:${XDG_RUNTIME_DIR}/pulse/native"
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix"
      - "${XDG_RUNTIME_DIR}:${XDG_RUNTIME_DIR}"
      - "/mnt/wslg:/mnt/wslg:ro" # WSLg audio/video paths (exist on WSLg)
